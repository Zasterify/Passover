def total(listNum):  
    """
    To define the function 'listNum'
    """
    listNum = [1, 2, 3, 4, 5]
    
    for x in listNum: # for every number (x) in list of number
        total = total + x  # add x to the total
        return x  # to return x  
        